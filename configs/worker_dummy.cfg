# Synthetic integration tests only; requires --allow-dummy --model /dev/null.
# Use an isolated Go Server process: its model identity differs from real models.
rules = chinese
komi = 7.5
nnBackend = dummybackend
nnMaxBatchSize = 16
nnCacheSizePowerOfTwo = 12
nnMutexPoolSizePowerOfTwo = 8
